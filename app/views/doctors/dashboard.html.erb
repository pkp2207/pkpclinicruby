<h1>Doctors#dashboard</h1>
<p>Find me in app/views/doctors/dashboard.html.erb</p>
<h1>Doctor's Dashboard</h1>

<h2>Patient Registration Over Time</h2>
<%= line_chart @patient_registration_data, xtitle: 'Days', ytitle: 'Patients' %>
<%= @patient_registration_data.inspect %> <!-- To check the data structure -->

<h2>List of All Patients</h2>
<table>
  <thead>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Date of Birth</th>
      <th>Address</th>
      <th>Date Registered</th>
    </tr>
  </thead>
  <tbody>
    <% @patients.each do |patient| %>
      <tr>
        <td><%= patient.first_name %></td>
        <td><%= patient.last_name %></td>
        <td><%= patient.dob %></td>
        <td><%= patient.address %></td>
        <td><%= patient.created_at.strftime("%d %B, %Y") %></td>
      </tr>
    <% end %>
  </tbody>
</table>
